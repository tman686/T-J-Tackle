# Copy to .env and fill in for Phase 2 (live database).
# Phase 1 does not require a running database — `prisma validate` checks the schema only.
DATABASE_URL="postgresql://user:password@localhost:5432/jttackle?schema=public"
