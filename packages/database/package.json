{
  "name": "@jt/database",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical manufacturing + commerce data model for T&J's Tackle (Prisma).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "validate": "DATABASE_URL=postgresql://user:pass@localhost:5432/jttackle DIRECT_URL=postgresql://user:pass@localhost:5432/jttackle prisma validate",
    "format": "prisma format",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "seed": "tsx prisma/seed.ts"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
